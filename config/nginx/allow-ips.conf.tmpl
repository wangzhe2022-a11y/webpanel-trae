# ============================================================================
# Panel access whitelist
# ----------------------------------------------------------------------------
# IPs listed here are allowed to access the WebPanel management UI.
# If this file contains "deny all;" (added by the installer when you set a
# whitelist), only the listed IPs can reach the panel — everyone else gets 403.
#
# After editing, run:  nginx -t && systemctl reload nginx
# ============================================================================
