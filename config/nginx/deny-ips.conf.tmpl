# ============================================================================
# Panel access deny list (auto-managed by the dashboard)
# ----------------------------------------------------------------------------
# Suspicious IPs are added here from the dashboard "Panel Access / Security"
# page. Entries are plain nginx "deny" directives.
#
# After editing manually, run:  nginx -t && systemctl reload nginx
# ============================================================================
